pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building on branch... $$AWS_ACCESS_KEY'
                echo 'on branch... $TESTING'
            }
        }
    }
}
